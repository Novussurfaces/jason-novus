services:
  novus-surfaces:
    build: .
    container_name: novus-surfaces
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - NODE_ENV=production
      - OLLAMA_URL=http://100.83.113.25:11434/api/chat
